{
  "name": "api",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/api/src",
  "targets": {
    "build": {
      "command": "cd apps/api && zip -r dist/lambda.zip src/",
      "outputs": ["{projectRoot}/dist"]
    }
  }
}
