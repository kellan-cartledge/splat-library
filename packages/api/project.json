{
  "name": "api",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "packages/api/src",
  "targets": {
    "build": {
      "command": "mkdir -p dist && cd src && zip -r ../dist/lambda.zip .",
      "options": { "cwd": "packages/api" },
      "outputs": ["{projectRoot}/dist"]
    }
  }
}
