{
  "name": "api",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "services/api/src",
  "targets": {
    "build": {
      "command": "mkdir -p dist && cd src && zip -r ../dist/lambda.zip .",
      "options": { "cwd": "services/api" },
      "outputs": ["{projectRoot}/dist"]
    }
  }
}
